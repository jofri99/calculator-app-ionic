import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'onexip-ionic-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
